алг ГауссаЗейделя (арг таб u, арг цел n, арг вещ eps)
    размерностьСетки := количествоПроцессоров - 1
    нц
        дельтаПроцессора := 0
        i := размерностьСетки - номерПроцессора
        j := 0
        нц для j до номерПроцессора-1
            текущаяПодматрица := подматрица(i, j)
            ОтправитьВерхнююСтроку(номерПроцессора+1)
            ПолучитьВерхнююСтроку(номерПроцессора+1)
            ПолучитьНижнююСтроку(номерПроцессора-1)
            АлгоритмГауссаЗейделя(текущаяПодматрица, 
                                  дельтаПроцессора)
            ОтправитьНижнююСтроку(номерПроцессора-1)
        кц
        текущаяПодматрица := подматрица(i, j)
        ОтправитьВерхнююСтроку(номерПроцессора+1)
        ПолучитьВерхнююСтроку(номерПроцессора+1)
        ПолучитьПравыйСтолбец(номерПроцессора+1)
        АлгоритмГауссаЗейделя(текущаяПодматрица, 
                              дельтаПроцессора)
        ОтправитьПравыйСтолбец(номерПроцессора+1)
        нц для i до размерностьСетки
            текущаяПодматрица := подматрица(i, j)
            ОтправитьЛевыйСтолбец(номерПроцессора-1)
            ПолучитьЛевыйСтолбец(номерПроцессора-1)
            ПолучитьПравыйСтолбец(номерПроцессора+1)
            АлгоритмГауссаЗейделя(текущаяПодматрица, 
                                  дельтаПроцессора)
            ОтправитьПравыйСтолбец(номерПроцессора+1)
        кц
        дельта = 
            МаксДельтаВсехПроцессоров(дельтаПроцессора)
    кц пока дельта > eps
кон